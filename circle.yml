machine:
  services:
    - docker

deployment:
  snapshot:
    branch: master
    commands:
      - docker build -t gmorpheme/foobar-txn .
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push gmorpheme/foobar-txn
